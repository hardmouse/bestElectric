import { Item } from '../Item';
import { AbstractResponsiveRowGallery } from './AbstractResponsiveRowGallery';
import { GalleryOptions, ModelAttributes } from './AbstractGallery';
export interface NaturalGalleryOptions extends GalleryOptions {
    rowHeight: number;
}
export declare class Natural<Model extends ModelAttributes = any> extends AbstractResponsiveRowGallery {
    protected defaultOptions: NaturalGalleryOptions;
    protected options: NaturalGalleryOptions;
    protected getEstimatedItemsPerRow(): number;
    protected getEstimatedRowsPerPage(): number;
    /**
     *
     * @param items
     * @param fromRow
     * @param toRow
     * @param currentRow
     */
    protected organizeItems(items: Item[], fromRow?: number, toRow?: number, currentRow?: number): void;
    private computeSizes(chunk, containerWidth, margin, row, maxRowHeight?);
    private getRowWidth(maxRowHeight, margin, items);
    private getRowHeight(containerWidth, margin, items);
    private getRatios(items);
    private getImageRatio(el);
    private apportionExcess(items, containerWidth, rowWidth);
}
